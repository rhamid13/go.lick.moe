install deps:

	go get github.com/gorilla/mux
	go get github.com/mattn/go-sqlite3

build:

	go build


keep in mind that go-sqlite uses cgo (relies on glibc), so static
linking is probably out of the question...